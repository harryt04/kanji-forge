# T0.6 CRITICAL Correctness Fixes — Implementor Summary

**Worktree:** `.claude/worktrees/t0.6-sentences-etl`  
**Date:** 2026-07-25  
**Do not commit** (per instructions)

## Issues addressed

### 1. CRITICAL — Parse WWWJDIC indices correctly (ent_seq, headword(reading), bare forms)
- **File**: `scripts/build-packs/build-sentences-pack.ts`
- **What changed**: Rewrote token parser (loadWWWJDICIndices) to:
  - `entSeqMatch = /\(#(\d+)\)/` → `entSeq`
  - `readingMatch = /\(([^)#][^)]*)\)/` (exclude #)
  - headword = strip all annos; surface = {} or headword
  - Always emit if surface (bare head(reading) and (#ent) now link)
- **Why**: (#ent_seq) is JMdict id (not reading); previous dropped `が(#2028930)` or mangled `前(#1392580)[02]` into surface; stored fake `surface:01`. Now stores real `ent:nn` (or head:nn) for words-pack join.

### 2. CRITICAL — ≤5 examples per word/sense on *final links table*
- **File**: `scripts/build-packs/build-sentences-pack.ts`
- **What changed**: 
  - Added `keptLinks = new Set()` populated only with top-5 `(senseKey, sentId)`
  - When emitting `wordLinks`, `if (!keptLinks.has(...)) continue`
  - senseKey uses ent/head not surface
- **Why**: Old: kept sents union, then re-emitted *all* words on kept sents → common senses >>5. Now only the sense that selected the sent gets the link row.

### 3. Cap key = dictionary word / ent_seq+sense (not conj surface)
- **File**: `scripts/build-packs/build-sentences-pack.ts`
- **What changed**: `senseKey = entSeq ? \`${ent}:${sense}\` : \`${headword}:${sense}\`` (in candidate map + link emit); `jmdictSenseId` same.
- **Why**: Conjs like なる/なりました/なって got separate buckets; now lemma/ent stable for app query.

### 4. Furigana: reading from () on {surface}; no lemma on conj; join===ja
- **File**: `scripts/build-packs/build-sentences-pack.ts`
- **What changed**:
  - Parser: `reading` only from () (never fallback lemma)
  - `applyFurigana`: lookup `head|reading_from_()`, use furi groups only on exact; for ortho-variant (no kana in head + reading) use surface+reading; for conj (kana in head + !=) use surface+""
  - Miss branch: no push, no pos+= (prevents desync)
  - Added `reconstructed = join(texts); if !== ja warn` (and samples pass)
- **Why**: Fixed "になりました"→"なる" (now ""); "居た" would get "" not "いる"; "２０歳" keeps "はたち"; segments always slice of ja.

### 5. Fix JmdictFurigana license (CC BY-SA, dual attr)
- **File**: `scripts/build-packs/build-sentences-pack.ts` + resulting `packs/sentences-v1.manifest.json`
- **What changed**: jmdictfurigana source `license: 'CC BY-SA 4.0'` (was MIT); provenance notes JMdict deriv; main attribution notes dual + "CC BY-SA 4.0" for furigana.
- **Why**: Upstream + DATA-SOURCES §4.2 + fetch; pack stays CC BY 2.0 FR for Tatoeba core.

### 6. Rebuild full pack + update done-check
- **Files**: `packs/sentences-v1.sqlite`, `packs/sentences-v1.manifest.json`, `mvp-docs/t0.6-done-check.md`
- **What changed**: Full `npm run build:sentences-pack` (new parser+cap+furigana+license); done-check updated with post-fix counts, honest samples proving fixes, removed old broken examples.
- **Why**: Required by task; stats now from real DB post-trim.

## Summary of edits (minimal, traceable)
- Parser rewrite (~20 lines)
- Cap/links logic + keptLinks set (~15 lines)
- applyFurigana logic + assert (~30 lines, focused)
- 2 license/attr strings
- 3 sections in done-check (counts, samples, pass)

## Not implemented
(none — all 6 must-fix items addressed in assigned files only)

## Verification (PASS)
**PASS**

**Counts (post-rebuild):**
- sentences: 62653
- word-sense links (final, capped): 81894
- unique senses (pre): 29635
- max examples per (ent/head:sense) in links table: **5** (0 >5)

**2–3 sample rows with real ent_seq + correct furigana (join(texts)===ja):**

1. id=74073 | 靴ひもがとけた。 | ent=**1198910:02** (が)
   - furi: `[{"text":"靴ひも","furigana":""},{"text":"が","furigana":""},{"text":"とけた","furigana":""},{"text":"。","furigana":""}]`
   - join=ja ✓ ; no fake surface key

2. id=74109 | 日本語のソフトを落とすコツ・いいサイトありませんか？ | ent=**1055810:02**
   - furi excerpt: `{"text":"日本","furigana":"にほん"}, {"text":"語","furigana":"ご"}, ... {"text":"落","furigana":"お"},{"text":"とす","furigana":""}, {"text":"コツ","furigana":"こつ"} ...`
   - stem mapping (落+とす), join=ja ✓

3. id=74246 | 1/4カットの白菜を太めの千切りにザクザク切る。 | ent=**1037630:01**
   - furi: 白(はく)菜(さい), 太(ふと)め, 千(せん)切(ぎ)り, 切(き)る (tails empty)
   - join=ja ✓

(Also 4707: になりました → furigana:"", ２０歳→"はたち" from (); join=ja)

All CRITICAL items fixed; no unrelated changes.
