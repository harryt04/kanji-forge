# Implementor Change Summary

## Issues addressed

### Assertion in build-kanji-pack.ts was broken (false positive on "skip" in meanings)
- **File**: `scripts/build-packs/build-kanji-pack.ts`
- **What changed**: Updated the CRITICAL assertion check (and related comments/logs) from naive `blob.includes('skip') || ...` to `blob.includes('qc_type="skip"') || ... || includes('skip_misclass')`. Also updated header docs.
- **Why**: The old check falsely triggered on legitimate English meanings containing the word "skip" (e.g. 飛 "skip (pages)"), preventing real pack build and leaving 0-byte sqlite. The qc data is never parsed into entries (only counted+omitted), so stricter attr checks ensure no leak of skip/misclass qc fields while allowing natural text. Now assertion + build passes.

### Stale / incorrect data in t0.3-done-check.md
- **File**: `mvp-docs/t0.3-done-check.md`
- **What changed**: Replaced build output capture, checks performed, deliverables, files changed, and command examples with real outputs from successful run (after fix). Updated counts (14050 stripped), SHA, spot-check details, verification notes (sqlite queries, 6 legit skip words, 0 bad attrs), and notes about the assertion fix.
- **Why**: Prior agent left placeholder 0-byte + stale numbers/details from failed/partial run. Task requires update with REAL command output and verification.

## Not implemented

(none; all listed tasks completed with minimal targeted edits + real build + verifies)

## Summary of run
- Ran pnpm install (via nvm pnpm + hoisted to resolve ancestor node_modules for resolution)
- Ran real build via pnpm dlx tsx ... (worktree cwd)
- Verified size 1966080 bytes >>0 , count 13108, assertion pass + independent sqlite3 queries (0 bad qc attrs in DB; schema has no skip/misclass cols/fields), spot checks passed (incl. 飛 with legit "skip (pages)")
- packs/ updated, no sources.lock touched, no commit, .salvage ignored
- 2 source files edited (ts + md); packs artifacts produced by build

PASS
kanji count: 13108
pack bytes: 1966080
sample rows:
  日: code=U+65E5 grade=1 strokes=4 freq=1 | on: ニチ, ジツ | kun: ひ, -び, -か | meanings: day; sun; Japan
  本: code=U+672C grade=1 strokes=5 freq=10 | on: ホン | kun: もと | meanings: book; present; main
  語: code=U+8A9E grade=2 strokes=14 freq=301 | on: ゴ | kun: かた.る, かた.らう | meanings: word; speech; language
  未: code=U+672A grade=4 strokes=5 freq=650 | on: ミ, ビ | kun: いま.だ, ま.だ, ひつじ | meanings: un-; not yet; hitherto
  末: code=U+672B grade=4 strokes=5 freq=456 | on: マツ, バツ | kun: すえ, うら, うれ | meanings: end; close; tip
files changed: scripts/build-packs/build-kanji-pack.ts, mvp-docs/t0.3-done-check.md (packs/kanji-v1.sqlite + .manifest.json produced by build)
