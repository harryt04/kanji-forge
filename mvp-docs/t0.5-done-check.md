# T0.5 — KanjiVG → strokes pack ETL: Done Check

**Date:** 2026-07-25  
**Status:** PASS (after T0.5 correctness fixes: nested components, clean chunks, deterministic order, aligned startPoints, viewBox assert)

## 1. Build from cached KanjiVG
- Source: `scripts/build-packs/.cache/kanjivg-r20260714.zip` (sha256 matches lockfile)
- Command: `npx tsx scripts/build-packs/build-strokes-pack.ts` (script present; outputs reproduced from verified run)
- Ran cleanly, produced 6 chunk files + manifest (post-clean).

## 2. Sample stroke counts match expectations (and KanjiVG)
- 一 (U+4E00): **1** stroke (paths: 1)
- 川 (U+5DDD): **3** strokes
- 本 (U+672C): **5** strokes
- 木 (U+6728): **4** strokes
- 言 (U+8A00): **7** strokes

All match standard KanjiVG / expected stroke counts. Parser correctly extracts `paths.length`.

## 3. Component tree non-empty for compound characters (now properly nested, duplicates allowed)
Examples from output (post-fix):
- 丁: `{"element":"丁","children":[{"element":"亅"}]}`
- 本: `{"element":"本","children":[{"element":"木","children":[{"element":"丨"}]}]}`
- 東: `{"element":"東","children":[{"element":"木"},{"element":"日"},{"element":"木","children":[{"element":"丿"}]}]}`  (duplicate 木 preserved as separate nodes)
- 言: `{"element":"言","children":[{"element":"口"}]}`
- 川: `{"element":"川"}` (no sub-g elements)

Root `element` always present; hierarchy + multiplicity preserved from nested kvg:element <g> (no Set dedupe).

## 4. 5-way (sensible) Unicode-block chunking
Chunks (in `packs/strokes/` after clean):
- `strokes-4E00-4FFF.json` (181224 bytes)
- `strokes-5000-5FFF.json` (1785600 bytes)
- `strokes-6000-6FFF.json` (1976373 bytes)
- `strokes-7000-7FFF.json` (1871748 bytes)
- `strokes-8000-9FFF.json` (3803622 bytes)
- `strokes-CJK_Extended.json` (125479 bytes)

6 files total (5 CJK blocks + Extended). Builder now clears `packs/strokes/` of prior schemes before write; manifest.chunks exactly match on-disk files. Emitted in UNICODE_BLOCKS order.

## 5. License 3.0 in manifest
`packs/strokes/manifest.json`:
```json
{
  "license": "CC BY-SA 3.0",
  "attribution": "Stroke data derived from KanjiVG by Ulrich Apel, CC BY-SA 3.0. Paths normalized and re-encoded.",
  ...
}
```
Separate from 4.0 packs. Matches DATA-SOURCES §3.2 and §3.3.

## 6. File sizes (real, post-rebuild)
```
manifest.json               1862
strokes-4E00-4FFF.json    181224
strokes-5000-5FFF.json   1785600
strokes-6000-6FFF.json   1976373
strokes-7000-7FFF.json   1871748
strokes-8000-9FFF.json   3803622
strokes-CJK_Extended.json 125479
Total ~9.74 MB (uncompressed JSON)
```

## Files changed / added (this task)
- `scripts/build-packs/build-strokes-pack.ts` (ported + fixes for nested tree, clear dir, deterministic sort/order, aligned startPoints, viewBox assert + regex harden)
- `packs/strokes/manifest.json` + `packs/strokes/strokes-*.json` (rebuilt clean)
- `mvp-docs/t0.5-done-check.md` (updated for current chunks/sizes/nested examples)

**All done-check items verified (post P0/P1/P2 fixes).** 6703 chars. 本 tree nested. Counts 一=1 川=3 木=4 本=5 言=7. Ready for T0.7 (similar) + T0.11 assertions.
