# T0.6 Sentences ETL - Implementor Summary

## Tasks completed
1. Fixed `scripts/build-packs/build-sentences-pack.ts` (parser + cap key)
2. pnpm install (via symlink reuse, already up to date)
3. Ran full build: `npx --yes tsx ...` (equiv pnpm dlx; pnpm exec not possible w/o dep change) -- completed, multi-min run
4. Verified: 41+ MB, real data, samples, links, furigana, manifest CC BY 2.0 FR
5. Updated `mvp-docs/t0.6-done-check.md` with real counts/samples
6. No sources.lock touched (timestamp old), no commit, .salvage only referenced, worktree only

## Verification results
- **PASS**
- sentence count: 62498
- pack size: 41.17 MB (packs/sentences-v1.sqlite)
- manifest license: CC BY 2.0 FR (no SA)
- samples:
  - ID 4707: JA="ムーリエルは２０歳になりました。" EN="Muiriel is 20 now." furi has "２０歳":"はたち" , links: "２０歳:01" pos 6-9 , "になりました:01" 9-15
  - ID 74695: JA="ちょっと、そこのきみ！" EN="Hey, you there!" links present with composites "きみ:01" etc.
  - ID 193379: "もちろんさ。" etc. (furigana + links present)
- unique sense keys: ~34k ; after cap ~33k ; links ~173k
- length filter, >=1 EN, inapp filter, <=5-per-sense all active and effective

## Files changed (worktree)
- scripts/build-packs/build-sentences-pack.ts (parser for surface/reading/sense from varied wwwjdic tokens; composite senseKey=`${surface}:${senseId}` for grouping and storage)
- mvp-docs/t0.6-done-check.md (real stats, samples from actual build output, updated notes)
- packs/sentences-v1.sqlite (new real build)
- packs/sentences-v1.manifest.json (new real metadata)

## Changes made (minimal, targeted)
- Replaced naive regex in loadWWWJDICIndices with token-split parser that extracts surface from { } (for position match + highlight), reading from ( ) or lemma, sense from [nn] regardless of position.
- Used composite senseKey for senseToCandidates and jmdictSenseId in links (fixes all-under-'01' bug that caused only ~4 sentences).
- Updated docs with actual build output (no guessing).

## Why fixes the stub
Prior 341B stub + 2-sentence manifest was symptom of broken per-sense cap (uniqueSenses=1, kept~4). Composite key + accurate surface extraction produces real volume matching spec intent (~62k vs prior est 42k; slight variance from ranking/overlap).

## Notes
- Build used cache via .cache symlink (from worktree).
- node_modules symlinked from root for module resolution (no pnpm install wrote files).
- Did not touch sources.lock.json or anything outside worktree.
- Ran full ETL end-to-end; no partials.
- Furigana partial (map hits + fallback) but present and plausible; links use positions for highlight.

PASS
62498
41.17 MB
samples: see above (4707, 74695 etc)
files: scripts/build-packs/build-sentences-pack.ts, mvp-docs/t0.6-done-check.md, packs/sentences-v1.sqlite, packs/sentences-v1.manifest.json
