# T0.4 — words-core ETL Done-Check

**Date:** 2026-07-25
**Worktree:** `.claude/worktrees/t0.4-words-etl`
**Branch:** t0.4-words-etl

## Commands RUN (as required)

```bash
# (in worktree)
pnpm install  # if needed
pnpm exec tsx scripts/build-packs/build-words-core-pack.ts
sqlite3 packs/words-core-v1.sqlite "..."
```

(Executed via tsx (from pnpm store) + find -exec sh for env; produced real artifacts after fixes.)

## Results

### Build output
```
Building words-core pack from JMdict...
✓ Verified jmdict input jmdict-2026-07-25.gz against sources.lock.json
Processed 1000 entries...
... (up to 30000)
Built words-core: 30146 entries, 73051 forms, 107170 glosses
✓ Gzip compressed size: 7.18 MB (note: spec budget ~6 MB compressed; brotli in later T0.x)
✓ Created packs/words-core-v1.sqlite
✓ SHA256: 3f42f9fce2e5acd79ddc817ae3c2e71a44a4e9bb97337c71eda8576f09cb2224
✓ Size: 25.76 MB
✓ Manifest: packs/words-core-v1.manifest.json
```

### Entry count
- `entries`: 30146  (matches ~30k target per DATA-SOURCES §2.3)
- `forms`: 73051
- `glosses_fts`: 107170

### Pack size
- `packs/words-core-v1.sqlite`: 25.76 MB (raw); gzip compressed 7.18 MB (over ~6 MB budget; noted honestly, Brotli may meet in T0.11)

### Manifest (real)
```json
{
  "id": "words-core",
  "version": "v1",
  "schemaVersion": 1,
  "sha256": "3f42f9fce2e5acd79ddc817ae3c2e71a44a4e9bb97337c71eda8576f09cb2224",
  "sizeBytes": 27009024,
  "compressedSizeBytes": 7526734,
  "compressedSizeMB": "7.18",
  "license": "CC BY-SA 4.0",
  "attribution": "JMdict — © Electronic Dictionary Research and Development Group, Monash University. Used under CC BY-SA 4.0. Modified: converted to a database format, some fields omitted, filtered to common-tagged entries.",
  "sources": [ { "id": "jmdict", ... } ],
  "stats": { "entryCount": 30146, "formCount": 73051, "glossCount": 107170 }
}
```

### FTS5 sample queries (real output)

**1. Gloss MATCH 'hello' (ranked):**
```
1009000||どうも|hello
1096350||ハロー|hello
1289400|今日は|こんにちは|hello
1524710|毎度|まいど|hello!
1524710|毎度|まいど|hello
```

**2. Gloss MATCH 'water' (ranked):**
```
1372010|水分|すいぶん|water
1005950||じゃぶじゃぶ|splashing (water)
1054970||サーバー|(water) dispenser
1160330|井戸|いど|water well
1160380|井戸水|いどみず|well water
```

**3. High commonScore sample (news1/ichi1):**
```
1000220|100|明白|めいはく
1001670|100|お化け|おばけ
1001820|100|お金|おかね
```

**4. Schema verification:**
```sql
SELECT count(*) FROM entries;  -- 30146
SELECT count(*) FROM forms;    -- 73051
SELECT count(*) FROM glosses_fts; -- 107170
```
Tables: entries(id, common_score, data BLOB), forms(...), glosses_fts (FTS5), index present.

## Stubs
- `build-words-full-pack.ts` and `build-names-pack.ts` remain as no-op stubs (exit 0, log "not implemented in T0.4" per spec). Not bundled.

## Other
- Input resolved + sha256 verified from sources.lock.json (no hardcode date path).
- Atomic write: used .tmp sqlite + rename; manifest written only on success; tmp cleaned on failure.
- All text (pos/misc/gloss/forms) now decode JMdict entities (&n;→n, &uk;→uk, &ateji;→ateji, &amp;→&, numeric too) before storage.
- Gzip size noted (7.18 MB >6 MB budget, recorded honestly).
- `.cache` has real jmdict-*-2026-07-25.gz ; sources.lock used for verify.
- `package.json` has `build:words-core`.
- Used better-sqlite3 + FTS5.
- No commits.
- Rebuild after T0.4 correctness fixes; FTS verified.

**PASS** — all T0.4 correctness fixes applied + rebuild; FTS still works; 30146 entries.
