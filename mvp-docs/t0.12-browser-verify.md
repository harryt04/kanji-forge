# T0.12 Browser Verification (independent)

**Date:** 2026-07-25  
**Method:** Playwright + Brave headless, `getImageData` pixel sampling  
**Build:** `pnpm build` static export OK; `pnpm test` 1/1 pass

## Results

| Check | Result | Evidence |
|---|---|---|
| Medium-zoom belt colors | **PASS** | 95 distinct quantized colors; top clusters match `--level-0..4` tokens (beige/gold/green/blue/dark), not uniform level-0 |
| Zoom-then-pan (dirty-rect invalidation) | **PASS** | After zoom+drag: midEmptyRatio=0; multi-level colors retained; band distinctness balanced (no mono strip artifact). Screenshot `t0.12-verify-after-pan.png` shows continuous multi-color wall |
| Small pans @ medium | **PASS** | Belt colors present; band distinct 7–11 across scanlines |
| Resize then pan @ medium | **PASS** | Isolated retest: after 1280→1600 resize, opaque pixels 1,185,600 / 0 transparent; pan keeps full coverage. Earlier FAIL was false (canvas intentionally empty in high-zoom DOM mode) |
| rAF / wheel burst | **PASS** | 20 synthetic wheels handled in ~15–22ms; page responsive |
| Passive wheel listener | **PASS** | 0 `preventDefault inside passive` console errors after native `{passive:false}` listener |
| High-zoom DOM mode | **PASS** | 84–98 kanji DOM nodes (e.g. 日本語…) |
| List fallback | **PASS** | List view button + content with kanji |

## CSS tokens observed
```
--level-0: #d9d2c3
--level-1: #e8b23d
--level-2: #6b9950
--level-3: #3d5a9e
--level-4: #1e1b18
```

## Screenshots
- `mvp-docs/t0.12-verify-initial.png`
- `mvp-docs/t0.12-verify-medium-zoom.png`
- `mvp-docs/t0.12-verify-after-pan.png`
- `mvp-docs/t0.12-verify-small-pan.png`
- `mvp-docs/t0.12-verify-high-zoom.png`
- `mvp-docs/t0.12-verify-resize-only.png`
- Raw JSON: `mvp-docs/t0.12-browser-verify.json`

## Residual / human
- On-device Android ≥50fps still required for GATE G-device / GATE 0 (not automatable here).
- `pnpm lint` currently broken on missing transitive ESLint packages after a lockfile refresh — separate from T0.12 logic; fix with dependency restore before GATE 0.
- Prototype only; productionize in Phase 2.

## Verdict
**T0.12 code-side review fixes: CLOSED** pending human device measurement.
