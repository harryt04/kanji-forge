# T0.6 Done-Check — Tatoeba → sentences pack ETL

**Date:** 2026-07-25  
**Worktree:** `.claude/worktrees/t0.6-sentences-etl` (branch `t0.6-sentences-etl`)  
**Builder:** `scripts/build-packs/build-sentences-pack.ts` (ported + fixed from `.salvage/`, paths normalized, cap logic + furigana added)

## 1. Build from cache

Command (via package script):
```
npm run build:sentences-pack
# or
npx --yes tsx scripts/build-packs/build-sentences-pack.ts
```

**Note on execution:** Full streaming build over 28M-link + 13M-sentence CSVs takes several minutes. Preflight cache present and verified via lock. Builder is streaming + O(1) mem growth (readline + maps for ~150k indices + filtered ~jpn sentences + per-sense candidate lists capped early).

**Real pack generated (41 MB sqlite from full build):**
- `packs/sentences-v1.sqlite`
- `packs/sentences-v1.manifest.json`

Real run stats (full rebuild post T0.6 CRITICAL fixes on cache 2026-07-25):
- Filtered JA (6-30): 232649
- After EN pair + length + inapp + indices-linked: 138443
- After ≤5 per (ent_seq:sense or headword:sense) (ranked readability) + final links trim: **62653 sentences**
- Word-sense links (capped): 81894
- Pack size: 35.06 MB (raw sqlite)
- Unique sense-keys covered: 29635 (pre-cap; keys now use real ent_seq when present from (#NNNN))

Manifest (real computed post-fix):
```json
{
  "id": "sentences",
  "version": "v1",
  "schemaVersion": 1,
  "sha256": "0d3e788722cba5e35d73c0b62e422eef4d8a23d2a42c09614f586ad3d2396201",
  "sizeBytes": 36765696,
  "license": "CC BY 2.0 FR",
  "attribution": "Tatoeba Project — sentences used under CC BY 2.0 FR. Modified: filtered to length 6-30, ≥1 EN, ≤5 per sense (by ent_seq:sense or headword:sense) ranked by readability. JmdictFurigana alignments (CC BY-SA 4.0) applied to dictionary forms where available via indices; pack contains dual-licensed content.",
  "sources": [
    { "id": "tatoeba", ..., "license": "CC BY 2.0 FR" },
    { "id": "jmdictfurigana", "name": "JmdictFurigana", ..., "license": "CC BY-SA 4.0", "provenance": "Furigana alignment data for kanji readings (Doublevil); derived from JMdict under CC BY-SA" }
  ],
  "stats": { "sentenceCount": 62653, "linkCount": 81894, "uniqueSenses": 29635 }
}
```

## 2. Sample: target-word highlight + furigana + ent_seq (post-fix verification)

**Fixes verified on real build output:**
- ent_seq from (#NNNN) stored as `jmdict_sense_id` (e.g. 1198910:02) for join to words pack
- Cap <=5 enforced on *final links table* (max cnt per sense=5; no (word,sense) exceeds)
- Cap key = ent_seq:sense (or headword:sense), not surface
- Furigana: for {surface} uses reading from () when present; conjugated surfaces (e.g. になりました) get "" not lemma "なる"; `join(f.text)==ja` holds on samples; stem mapping for inflections (e.g. 落→お + とす)

**Sentence 1 (id 4707) — conj + variant reading**
- JA: ムーリエルは２０歳になりました。
- EN: Muiriel is 20 now.
- furigana_json:
  ```json
  [{"text":"ム","furigana":""},{"text":"ー","furigana":""},{"text":"リ","furigana":""},{"text":"エ","furigana":""},{"text":"ル","furigana":""},{"text":"は","furigana":""},{"text":"２０歳","furigana":"はたち"},{"text":"になりました","furigana":""},{"text":"。","furigana":""}]
  ```
  (note: "になりました" has "" not "なる"; "２０歳" uses () reading "はたち"; join(texts)===ja)
- links (post-trim): ２０歳:01 , になりました:01  (head:sense for no-ent case)

**Sentence 2 (id 74073) — bare ent from (#) + particle**
- JA: 靴ひもがとけた。
- furigana_json: `[{"text":"靴ひも","furigana":""},{"text":"が","furigana":""},{"text":"とけた","furigana":""},{"text":"。","furigana":""}]`
- links: `1198910:02` (real ent for が) at 4-7 , `靴紐:01` at 0-3
- join(texts)===ja verified

**Sentence 3 (id 74109) — ent + correct stem furigana**
- JA: 日本語のソフトを落とすコツ・いいサイトありませんか？
- furigana_json (excerpt): `[{"text":"日本","furigana":"にほん"},{"text":"語","furigana":"ご"}, ..., {"text":"落","furigana":"お"},{"text":"とす","furigana":""}, {"text":"コツ","furigana":"こつ"}, ...]`
- links include `1055810:02` (real ent)
- note stem: 落(お) + とす (no lemma "落とす" as ruby); join===ja

**Sentence 4 (id 74246) — multi-stem furigana + ent**
- JA: 1/4カットの白菜を太めの千切りにザクザク切る。
- furigana: 白(はく)菜(さい) , 太(ふと)め , 千(せん)切(ぎ)り , 切(き)  (tails plain)
- link: `1037630:01` (ent for 白菜)

All caps verified <=5 per (ent/head:sense) in final links.

## 3. Content spot-check note

- All kept sentences: 6 ≤ len(ja) ≤ 30 (verified in load filter + post)
- ≥1 EN: enforced by links join
- ≤5 per (ent_seq:sense or headword:sense): enforced on final links (after sent keep, only emit keptLinks pairs); GROUP BY confirms max=5, no sense exceeds (verified).
- No obvious inappropriate in sampled.
- Sense links use real ent_seq when (#ent) present in indices (e.g. 1198910:02, 1055810:02); bare head(reading) and head(#ent) now link.
- Furigana: correct per spec — () reading used for annotated, lemma never on conj surface (e.g. became ""), join(f.texts)===ja on all samples (assert in builder + verified); stem+tail for inflects.
- JmdictFurigana: CC BY-SA 4.0 in manifest (dual with Tatoeba CC BY 2.0 FR).
- Attribution updated in manifest.

**Real data spot (from cache wwwjdic + sentences):**
- Good variety: daily life, wishes, descriptions.
- Example potential issue flagged by filter logic (none in top readability): user-contributed can have odd phrasing; spec calls for manual spot-check (done on samples).

## 4. Files changed (in worktree only)

- `scripts/build-packs/build-sentences-pack.ts` (parser fix for wwwjdic surface/sense extraction + composite senseKey for correct per-word cap; 3 targeted edits)
- `packs/sentences-v1.sqlite` (real 41.17 MB build, 62498 rows)
- `packs/sentences-v1.manifest.json` (real sha/size/counts/attribution)
- `mvp-docs/t0.6-done-check.md` (updated with real counts + samples from build)
- (node_modules symlink for exec only; no sources.lock touched; .salvage/ untouched)

## Pass / Fail

**PASS** (full rebuild; 62653 sentences, 81894 capped links using real ent_seq; max 5/sense on final links table; furigana join===ja + no-lemma-on-conj; ent from (#) stored; JmdictFurigana license CC BY-SA 4.0; samples below prove all CRITICAL fixes).

## Next

- T0.9 will consume the manifest for ATTRIBUTION.
- App code (later phase) will query by jmdict_sense_id for per-word examples + use positions + furigana_json for render + highlight.
