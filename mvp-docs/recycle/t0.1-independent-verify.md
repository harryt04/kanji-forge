# T0.1 Security Fix Independent Verification
**Worktree:** `/Users/harry/Documents/git/kanji-forge/.claude/worktrees/t0.1-security-fix`
**Date:** 2026-07-25
**Verifier:** Independent (non-prior-agent)

## 1. `scripts/build-packs/fetch-sources.ts`
**Status: PASS**

Evidence:
- No hardcoded fake `licenseHash` (e.g. `'cc-by-sa-4.0-edrdg'`). The only mention is in `assertPlaceholderRejected()` which explicitly rejects it.
- License hashes are **always computed** via `computeLicenseHash()` → `crypto.createHash('sha256')` on actual license text (cached or fetched).
- Uses `https` protocol detection for all fetches (EDRDG, GitHub, Tatoeba, etc.).
- Strong redirect allowlist: `ALLOWED_HOSTS` includes only `ftp.edrdg.org`, `www.edrdg.org`, `github.com`, `raw.githubusercontent.com`, `downloads.tatoeba.org`, `creativecommons.org`, etc. Redirects are validated with `isAllowedHost()`.
- Path sanitization: `sanitizeForPath()` strips `../`, path separators, and non-whitelisted chars.
- Added validation that refuses to write lockfile if any `licenseHash` is not 64-hex.
- Includes unit assertion rejecting placeholder values.

## 2. `sources.lock.json` — licenseHash format
**Status: PASS**

All 11 entries have valid 64-character hexadecimal `licenseHash` values. No placeholders or malformed strings found.

Examples:
- EDRDG entries (kanjidic2, jmdict, etc.): `bf403f307237ede827bca3be4e6bd84b7e42890d0f4d8f515548df74a74e0640`
- kanjivg: `d255e07978fd16ddfec38bc59dc9d857b885dd44ddbf4e79baf207d30746bdcc`
- tatoeba: `58be7312a781d26af1709feda2ac63b3655b71694b7fe77c06bf1d33048ddf21`
- jlpt-vocab-yomitan: `5704a57be1e85040c709008c5d643a78f6e6db9ebadbd85d5bfa62127a0a5ef4`

## 3. Spot-check of license hashes
**Status: PASS**

Verified 3 license cache files against lockfile:

- `edrdg-licence-edrdg.txt` → `bf403f307237ede827bca3be4e6bd84b7e42890d0f4d8f515548df74a74e0640` (matches all EDRDG entries)
- `kanjivg-license.txt` → `d255e07978fd16ddfec38bc59dc9d857b885dd44ddbf4e79baf207d30746bdcc` (matches)
- `jlpt-vocab-yomitan-license.txt` → `5704a57be1e85040c709008c5d643a78f6e6db9ebadbd85d5bfa62127a0a5ef4` (matches)

Computed via `crypto.createHash('sha256')` — identical to lockfile.

## 4. Script execution test
**Status: PASS (safe subset)**

- Full run of `pnpm exec tsx scripts/build-packs/fetch-sources.ts` avoided (would re-download >1GB of data including Tatoeba CSVs).
- Ran safe validation:
  - Confirmed script loads, cache/lockfile present.
  - Node assertion: all `licenseHash` values are valid 64-hex → `true`.
  - Script contains proper guards, allowlists, and hash validation before writing lockfile.

## Overall Result
**PASS**

All T0.1 security requirements satisfied. The worktree implements real license hashing, host/redirect allowlisting, path sanitization, and validation. No evidence of prior placeholder vulnerabilities remains.

**Key Evidence Files:**
- `scripts/build-packs/fetch-sources.ts` (lines 274–292, 43–57, 59–65, 606–611)
- `scripts/build-packs/sources.lock.json`
- Cache: `scripts/build-packs/.cache/*-license.txt`

No changes were made to the worktree during verification.
