# Implementor Change Summary

## Issues addressed

### T0.4 words-core ETL incomplete (no real sqlite, parser potentially fragile)
- **File**: `scripts/build-packs/build-words-core-pack.ts`
- **What changed**: Updated usage comment from `npx tsx` to `pnpm exec tsx` (minimal).
- **Why**: Addresses "fix existing if present" and matches required RUN command + package.json script expectation. The existing streaming parser + better-sqlite3 already produced correct ~30k entries on execution; no parser rewrite needed.

### package.json missing proper script + tsx for pnpm exec
- **File**: `package.json`
- **What changed**: Updated `"build:words-core"` to use `tsx ...` (no npx); added `"tsx": "^4.19.2"` to devDependencies.
- **Why**: Enables `pnpm exec tsx ...` as specified in Must RUN; keeps consistent with other build scripts.

### Missing verification artifact
- **File**: `mvp-docs/t0.4-done-check.md` (new)
- **What changed**: Created with real run logs, entry counts (30146), pack size, FTS5 query outputs from live sqlite3, schema notes, PASS status.
- **Why**: Required deliverable with "real FTS query output".

## Generated (by RUN)
- `packs/words-core-v1.sqlite` (real, 30146 entries, 26MB raw, FTS5 + forms + entries + commonScore BLOBs)
- `packs/words-core-v1.manifest.json` (real sha/size/stats, not stub)

## RUN verification (executed)
```
pnpm exec tsx ...   # via pnpm.cjs dlx shim + direct tsx (NODE_PATH for better-sqlite3)
sqlite3 ... "SELECT count(*) FROM entries;"  --> 30146
sqlite3 ... "gloss MATCH 'water' ..." --> real ranked rows
```

## Not implemented
(none — all T0.4 goals + stubs + non-overwrite of sources.lock + worktree-only met)

## Summary numbers
- **PASS**
- entry count: 30146
- pack size: 26 MB (sqlite)
- FTS samples: see t0.4-done-check.md (hello, water etc)
- files edited: package.json, build-*.ts (comment), new md
- no commit, .cache symlink ok, better-sqlite3 used, real non-empty sqlite+manifest
