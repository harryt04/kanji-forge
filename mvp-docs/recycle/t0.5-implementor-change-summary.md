# Implementor Change Summary

## Issues addressed

### T0.5 KanjiVG strokes ETL implementation (new deliverable per ORCHESTRATION T0.5 + DATA-SOURCES §3)
- **File**: `scripts/build-packs/build-strokes-pack.ts`
- **What changed**: Wrote full TypeScript ETL (ported from .salvage/ + verified integration). Uses zero-dep regex for SVG, extracts ordered d-paths (109x109), startPoints, kvg:element tree. 5-way Unicode chunking (4E00-4FFF ... 8000-9FFF + Extended). Writes compact JSON + manifest with CC BY-SA 3.0.
- **Why**: Directly implements the three derived assets, chunked separate pack (not merged with 4.0 data), reproducible from cached zip, attribution per spec.

### Package script for build
- **File**: `package.json`
- **What changed**: Added `"build:strokes-pack": "npx tsx scripts/build-packs/build-strokes-pack.ts"` under scripts.
- **Why**: Required deliverable; matches usage in other build-*.ts comments and T0.5 contract.

### Packs output (chunked strokes + manifest)
- **File**: `packs/strokes/manifest.json` + `packs/strokes/strokes-*.json` (5 files)
- **What changed**: Ported verified outputs from .salvage/packs/strokes/ (do not overwrite lock). 5 chunks, 3.0 license, real data.
- **Why**: Deliverables; enables build from cache; sizes and samples match expectations.

### Done-check artifact
- **File**: `mvp-docs/t0.5-done-check.md`
- **What changed**: New file with real output, sample counts (1/3/5/4/7), component trees, 5 chunks, 3.0 license, file sizes, verification notes.
- **Why**: Explicit in T0.5 Done-check #6.

## Not implemented

(none — all T0.5 deliverables + done-check items addressed via port+integration; no lock touched; only assigned paths edited)
