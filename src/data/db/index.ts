/**
 * Local user SQLite/PGlite schema and migrations.
 *
 * TODO(T1.0): Initialize local DB with tables:
 *   - decks, deckMembership, cardStates, reviews, sessions, settings, dailyStats, outbox
 */

export const DB_STUB = true;
