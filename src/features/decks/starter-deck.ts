/** The deck a fresh user lands on. Progress for any other derived deck a
 * user previously started (e.g. the legacy `dev-kanji` fixture deck) is
 * left untouched — see `loadDeck`'s handling of missing definitions. */
export const STARTER_DECK_ID = 'jlpt-kanji-n5'
export const DEFAULT_STARTER_DECK_NAME = 'JLPT Kanji N5'
